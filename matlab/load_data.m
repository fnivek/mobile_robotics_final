function [source_pc, target_pc] = load_data(filename)
    % Loads a source and target point cloud
    %   filename - file name of the data to load
    source_pc = zeros(3, 1);
    target_pc = zeros(3, 1);
end
