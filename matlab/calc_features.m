function feats = calc_features(pc)
    % Calculate the curvature features
    %   pc - the input point cloud
    %   feats - the calculated features
    [~, n] = size(pc);
    feats = zeros(11, n);
end
